# ============================================================
# Claude Telegram Relay — Environment Variables
# ============================================================
# Copy to .env:  cp .env.example .env
# Then fill in your values below.
# ============================================================

# --- REQUIRED: Telegram ---

# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather

# Your Telegram user ID (from @userinfobot)
TELEGRAM_USER_ID=your_telegram_user_id

# --- REQUIRED: Supabase (persistent memory) ---

# Project URL (looks like https://abc123.supabase.co)
SUPABASE_URL=your_project_url

# anon public key (starts with eyJ...)
SUPABASE_ANON_KEY=your_anon_key

# Service role key — used to bypass RLS for backend writes.
# Find in Supabase Dashboard > Project Settings > API > service_role (secret).
# NEVER expose this in client-side code. chmod 600 your .env file.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# --- RECOMMENDED: Personalization ---

# Your first name
USER_NAME=Your Name

# Your timezone (IANA format: America/New_York, Europe/Berlin, etc.)
USER_TIMEZONE=UTC

# --- RECOMMENDED: Brain Directory ---

# Brain directory — where your agent's identity, memory, goals live
# Default: ~/clawdia (create with: mkdir -p ~/clawdia)
# BRAIN_DIR=~/my-agent

# --- OPTIONAL: Paths ---

# Relay data directory (defaults to ~/.claude-relay)
# RELAY_DIR=~/.claude-relay

# Heartbeat state directory (defaults to ~/.claude-relay)
# STATE_DIR=~/.claude-relay

# --- OPTIONAL: Oura Ring ---

# Personal access token from cloud.ouraring.com/personal-access-tokens
# OURA_API_TOKEN=your_oura_token

# --- OPTIONAL: Voice Transcription ---

# Voice provider: "groq" (free cloud API) or "local" (whisper.cpp on your machine)
# VOICE_PROVIDER=groq

# For Groq: Free API key from console.groq.com
# GROQ_API_KEY=your_groq_key

# For local whisper.cpp:
# WHISPER_BINARY=whisper-cpp
# WHISPER_MODEL_PATH=~/whisper-models/ggml-base.en.bin

# --- OPTIONAL: Session Sync ---

# Sync all Claude CLI projects to Supabase (default: only ~/clawdia project)
# SYNC_ALL_PROJECTS=true

# --- OPTIONAL: Tools (MCP Servers) ---

# GitHub personal access token (repo scope) — for GitHub MCP server
# GITHUB_PAT=your_github_pat

# Vercel API token — for Vercel MCP server
# VERCEL_API_TOKEN=your_vercel_token

# --- OPTIONAL: ElevenLabs (Driving Mode TTS) ---

# ELEVENLABS_API_KEY=your_elevenlabs_key
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
